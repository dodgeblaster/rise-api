export declare type RefreshTokenInput = {
    clientId: string;
    userPoolId: string;
    refreshToken: string;
};
export declare type RefreshTokenOutput = {
    accessToken: string;
    refreshToken: string;
    idToken: string;
};
export declare function refreshTokens(props: RefreshTokenInput): Promise<RefreshTokenOutput>;
