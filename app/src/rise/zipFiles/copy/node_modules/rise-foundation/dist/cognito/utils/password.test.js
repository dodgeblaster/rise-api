"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var password_1 = require("./password");
function containsUpperCase(str) {
    var res = false;
    str.split('').forEach(function (x) {
        if (x === x.toUpperCase()) {
            res = true;
        }
    });
    return res;
}
function containsNumbers(str) {
    return /\d/.test(str);
}
function containsSpecialCharacter(str) {
    return /[^A-Za-z0-9 ]/.test(str);
}
test('makePassword will make a password', function () {
    var pass = (0, password_1.makePassword)();
    expect(typeof pass).toBe('string');
    expect(containsUpperCase(pass)).toBe(true);
    expect(containsSpecialCharacter(pass)).toBe(true);
    expect(containsNumbers(pass)).toBe(true);
});
