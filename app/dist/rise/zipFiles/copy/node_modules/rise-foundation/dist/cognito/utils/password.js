"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.makePassword = void 0;
var uuid_1 = require("./uuid");
function makePassword() {
    var id = (0, uuid_1.uuid)().split('-').join('').slice(0, 10);
    var addCharacter = function (x, char) {
        var i = Math.floor(Math.random() * 10) + 1;
        var arr = x.split('');
        arr.splice(i, 0, char);
        return arr.join('');
    };
    var withUppercaseLetter = addCharacter(id, 'C');
    var withSpecialCharacter = addCharacter(withUppercaseLetter, '!');
    return withSpecialCharacter;
}
exports.makePassword = makePassword;
