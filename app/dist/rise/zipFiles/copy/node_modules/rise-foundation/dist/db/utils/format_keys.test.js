"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var format_keys_1 = require("./format_keys");
test('format keys will return the same thing if no @id keyword is present ', function () {
    var res = (0, format_keys_1.formatKeys)({
        pk: 'note',
        sk: 'note_1234'
    });
    expect(res).toEqual({ pk: 'note', sk: 'note_1234' });
});
test('format keys will replace @id with a unique id ', function () {
    var res = (0, format_keys_1.formatKeys)({
        pk: 'note',
        sk: 'note_@id'
    });
    expect(res.pk).toBe('note');
    expect(res.sk).not.toBe('note_@id');
    expect(res.sk.startsWith('note_')).toBeTruthy();
});
