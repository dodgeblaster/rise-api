export declare type LoginUserInput = {
    clientId: string;
    userPoolId: string;
    userName: string;
    password: string;
};
declare type OutputSuccessful = {
    accessToken: string;
    refreshToken: string;
    idToken: string;
};
declare type OutputNewPassword = {
    challenge: 'NEW_PASSWORD';
    session: string;
    advice: 'Should use loginHandleNewPassword rise function in response';
};
export declare type LoginUserOutput = OutputSuccessful | OutputNewPassword;
export declare function loginUser(props: LoginUserInput): Promise<LoginUserOutput>;
export {};
