export const permissions: {
    Effect: string;
    Action: string;
    Resource: string;
}[];
export const trigger: string;
export namespace alarm {
    const snsTopic: string;
}
export namespace env {
    const bucket: string;
}
